server:
  port: 8888

spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/aliciaVillon/ms-admin-server-data.git
          clone-on-start: true
          default-label: master
