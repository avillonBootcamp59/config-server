server:
  port: 8888

spring:
  application:
    name: config-server
  security:
    user:
      name: root
      password: s3cr3t
  cloud:
    config:
      server:
        git:
          uri: https://github.com/avillonBootcamp59/ms-admin-server-data
          clone-on-start: true
          default-label: master
          search-paths: {application}
  profiles:
    active: local
management:
  endpoints:
    web:
      exposure:
        include: "*"
encrypt:
  key: padfbabadfbaefbadfbaertbnhbfmhgijluio004pazsckp